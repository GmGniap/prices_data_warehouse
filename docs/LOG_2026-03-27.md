# Architectural Changelog - March 27, 2026

We executed a comprehensive modernization of the Data Warehouse mapping automated extraction components onto secure, robust, dependency-injected frameworks natively optimized for local debugging and cloud CI/CD execution flawlessly mapping to explicit Idempotency thresholds effectively.

---

### 1. Secure Modular Configurations (JSON Secrets Infrastructure)
- **Deprecating Hardcoded Maps:** Refactored away entirely from storing static connection layers inside `constants.py`.
- **Dynamic File Ingestion:** Implemented a hidden local `source_config.json` architecture explicitly filtered by `.gitignore` mapping DB tables natively back through `config/__init__.py`. 
- **GitHub Secret Tunneling:** Added environment-agnostic automated fallback protocols seamlessly hooking GitHub Action runner OS values to the internal extraction variable (`SOURCE_CONFIG_JSON`) automatically overriding absent text files natively! 

### 2. Advanced Scraping & Automation Infrastructure
- **Smart Rate Limiting:** Implemented an implicit `TokenBucket` extraction algorithm directly inside scraper execution loops to manually control outgoing requests organically smoothing server-load impact natively cleanly!
- **Fail-Safe Exponential Backoff:** Inserted a targeted `@retry_with_backoff` decorator proactively capturing ephemeral connection dropouts forcing controlled programmatic pauses inherently guaranteeing resilience dynamically.

### 3. Idempotency & Database Integrity
- **Watermark Source Metadata:** Established a `WatermarkLog` table tracking source activity (`greenway`, `wisarra`, `max`) mapped explicitly cleanly tracing execution.
- **Duplicate Execution Prevention:** Designed `run_all_scrapers.py` with embedded SQL pre-checks validating specific local execution logs dynamically preventing data overlap natively prior to launching scraper methods implicitly safely.

### 4. MaxMyanmar Execution
- **Framework Synchronization:** Rewrote the procedural script formatting cleanly onto the standard `MaxMyanmarScraper(db_manager=db)` OOP framework perfectly aligning with token buckets structurally.
- **Full Automation:** Re-assigned MaxMyanmar natively directly to parallel pipeline structures inherently binding to central execution logic automatically correctly identically to Greenway natively cleanly!

### 5. Architectural Database CLI Toolkit
- **Targeted Executions:** Created `cli.py` mapped intrinsically down through decoupled generic database commands isolating connection rules directly overriding native extraction points programmatically.
- **Control Execution Parameters Built:** 
    - `--init-db`: Bootstrap database schema inherently cleanly correctly natively securely.
    - `--reset`: Safely evaluate structurally mapped components implicitly replacing existing logic gracefully.
    - `--truncate`: Wipe tables cleanly keeping schema organically cleanly structurally.
    - `--health`: Seamlessly ping target connections returning Boolean metrics smoothly cleanly reliably explicitly safely.
  
### 6. Granular CI/CD GitHub Action Pipelines
- **Isolating Jobs:** Segmented the monolithic daily schedule organically explicitly down natively generating three cleanly staggered, distinct pipelines (`scrape_greenway.yml`, `scrape_wisarra.yml`, `scrape_maxmyanmar.yml`) isolating faults efficiently!
- **Pipeline Caching:** Assigned `cache: 'pip'` logically explicitly inside setup hooks triggering GitHub's explicit generic runner virtual-environment snapshot hashing guaranteeing blazing fast subsequent build times automatically skipping `requirements.txt` structurally!
